Python — — —

# Add to your MCP config (Cursor/VS Code/Claude):
```
{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "fc-1d9aa426e27f4c11b575c9279229a712"
      }
    }
  }
}
```

# Then use this prompt:
"Search for 'Search for content related to author Cassandra Crossno, U.S.-based writer associated with grief-focused content. Author of HOLY SH*T, THEY’RE GONE: Navigating the F*cking Aftermath of Loss Without the Bullsh*t ' and return 500 results with content in markdown, summary, links, html, screenshot, json, images format"

— — — — —
Node.js — — —

# Add to your MCP config (Cursor/VS Code/Claude):
```
{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "fc-1d9aa426e27f4c11b575c9279229a712"
      }
    }
  }
}
```

# Then use this prompt:
"Search for 'Search for content related to author Cassandra Crossno, U.S.-based writer associated with grief-focused content. Author of HOLY SH*T, THEY’RE GONE: Navigating the F*cking Aftermath of Loss Without the Bullsh*t ' and return 500 results with content in markdown, summary, links, html, screenshot, json, images format"

— — — — —
Go — — —

# Add to your MCP config (Cursor/VS Code/Claude):
```
{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "fc-1d9aa426e27f4c11b575c9279229a712"
      }
    }
  }
}
```

# Then use this prompt:
"Search for 'Search for content related to author Cassandra Crossno, U.S.-based writer associated with grief-focused content. Author of HOLY SH*T, THEY’RE GONE: Navigating the F*cking Aftermath of Loss Without the Bullsh*t ' and return 500 results with content in markdown, summary, links, html, screenshot, json, images format"

— — — — —
Java — — — 

# Add to your MCP config (Cursor/VS Code/Claude):
```
{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "fc-1d9aa426e27f4c11b575c9279229a712"
      }
    }
  }
}
```

# Then use this prompt:
"Search for 'Search for content related to author Cassandra Crossno, U.S.-based writer associated with grief-focused content. Author of HOLY SH*T, THEY’RE GONE: Navigating the F*cking Aftermath of Loss Without the Bullsh*t ' and return 500 results with content in markdown, summary, links, html, screenshot, json, images format"

— — — — —
Ruby — — —

# Add to your MCP config (Cursor/VS Code/Claude):
```
{
  "mcpServers": {
    "firecrawl-mcp": {
      "command": "npx",
      "args": ["-y", "firecrawl-mcp"],
      "env": {
        "FIRECRAWL_API_KEY": "fc-1d9aa426e27f4c11b575c9279229a712"
      }
    }
  }
}

```
# Then use this prompt:
"Search for 'Search for content related to author Cassandra Crossno, U.S.-based writer associated with grief-focused content. Author of HOLY SH*T, THEY’RE GONE: Navigating the F*cking Aftermath of Loss Without the Bullsh*t ' and return 500 results with content in markdown, summary, links, html, screenshot, json, images format"